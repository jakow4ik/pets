<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    >

    <LinearLayout
        android:id="@+id/detailSectionItemsLL"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="@dimen/big_padding"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/detailSectionTitleTV"
        />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/detailSectionTitleTV"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/big_padding"
        android:fontFamily="@font/raleway_medium"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/big_padding"
        android:paddingVertical="@dimen/big_padding"
        android:textColor="@color/header_text"
        android:textSize="@dimen/detail_section_title_text_size"
        app:drawableEndCompat="@drawable/ic_arrow_selector"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Section title"
        />

</androidx.constraintlayout.widget.ConstraintLayout>